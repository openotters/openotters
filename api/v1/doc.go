// Package v1 contains the protoc-generated Go bindings for the
// openotters daemon gRPC API (`openotters.daemon.v1`). The proto
// source of truth is `daemon.proto`; the *.pb.go and *_grpc.pb.go
// files are generated and should not be hand-edited — re-run
// `protoc --go_out=. --go-grpc_out=. ...` after a proto change.
package daemonv1
