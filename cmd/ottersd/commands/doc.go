// Package commands holds the kong-driven subcommands of the ottersd
// daemon — currently just `serve`, which wires the embedded
// registry, provider registry, state store, daemon, gRPC server,
// and signal handling into a single boot sequence.
package commands
