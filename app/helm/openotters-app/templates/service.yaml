apiVersion: v1
kind: Service
metadata:
  name: {{ include "openotters-app.fullname" . }}
  labels:
    {{- include "openotters-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "openotters-app.selectorLabels" . | nindent 4 }}
