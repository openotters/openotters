apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openotters-app.fullname" . }}-test-connection"
  labels:
    {{- include "openotters-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openotters-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
